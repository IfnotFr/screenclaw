import dotenv from "dotenv";

dotenv.config();

/**
 * Global Project Configuration
 */
export const config = {
  screen: {
    id: "",
    xOffset: 0,
    yOffset: 0,
    width: 1920,
    height: 1080,
  },
};
